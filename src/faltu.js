// user.save().then(()=>{
    //     res.status(201).send(user)
    // }).catch((e)=>{
    //     res.status(400).send(e)
    //     // res.send(e)
    // })

        // task.save().then(()=>{
    //     res.status(201).send(task)            //201 is used if something is created
    // }).catch((e)=>{
    //     res.status(400).send(e)
    // })

    // User.find({}).then((us)=>{
    //     res.send(us)
    // }).catch((e)=>{
    //      res.status(500).send()
    // })

      // User.findById(_id).then((user)=>{



    //  if(!user){
    //      return res.status(404).send()
    //  }
    //  res.send(user)
    // }).catch((e)=>{
    //  res.status(500).send()
    // })

    // Task.find({}).then((task)=>{
    //     res.send(task)
    // }).catch((e)=>{
    //     res.status(500).send()
    // })

    //     const _id=req.params.id
//     Task.findById(_id).then((tasks)=>{
//        if(!tasks){
//           return res.status(404).send()
//        }
//        res.send(tasks)
//     }).catch((e)=>{
//     res.status(500).send()
// })